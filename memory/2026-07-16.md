# 2026-07-16 — Park Point

## Status: PARKED at user request

### Git
- Branch: `feat/emr-layer1`
- HEAD: `9d6d3db` (docs: lock in frontend stack decisions)
- Clean working tree, pushed to origin. Nothing uncommitted.

### Completed (all committed + pushed)
- **Backend main SaaS spec** (Tasks 1-24): all modules, security hardening. Checkpoint 19 PASSED (85.47%, 1146 tests).
- **EMR Hospital System** (Tasks 1-18, 3 layers): all checkpoints PASSED. Layer 3 = 109 coverage tests all green (`f9a6fcc`).
- **ML Engine** (Tasks 20-22): full 5-layer risk + forecast pipeline built locally, runs <2ms with graceful degradation. Checkpoint PASSED (19 tests, `c811fc3`). All 8 patent claims implemented. Provisional patent filed (App #64/080,250).
- **Frontend stack decisions**: locked in `FRONTEND_STACK_DECISIONS.md` (`9d6d3db`). React 18 + TS + Vite + Tailwind + shadcn/ui + Framer Motion + Recharts + TanStack Query/Table + Zustand + cmdk + React Router v6 + RHF + Zod + react-i18next. Bundle budget ~145KB.

### NEXT UP (Task 4 — frontend, in-progress)
- Write the frontend builder brief. Split into 2 builds:
  1. **Clinical App** (`app.prescphealth.com`) — 22 screens, Phases 1-4. One login, role determines interface after auth.
  2. **Patient Portal** (`portal.prescphealth.com`) — 7 screens, Phase 5. Separate URL, no MFA, simpler UI.
- Brief must include: full locked stack, all coding standards (steering rules), 27 screen specs across 4 roles, API connection details (`api.prescphealth.com`), 10 premium UX patterns, color palette (risk strata colors from accessibility.md).
- Then hand to builder agent using same staging->import->checkpoint pattern as backend.
- NO frontend code written yet — only planning done.

### Standing rules (do not forget)
- 100% test pass rate, zero tolerance. 85% coverage min per checkpoint.
- Property tests must NOT allocate >1MB; mock asyncio.sleep to avoid retry hangs.
- Run backend tests from `backend/`, ML tests from root.
- PostgreSQL: localhost:5432, db `prescphealth_test`, user `postgres`, pw `2026victory`.
- No PHI in logs/errors. HIPAA cache headers on PHI responses. RLS on tenant tables. Decimal for money.
- Solo dev, quality non-negotiable. Global platform (must work on 3G + advanced markets).


## DECISION (2026-07-16): Patent/paper track STOPPED — product-only from here
- User confirmed: paper and patent work has stopped for now. Everything is about the PRODUCT.
- Resolves the earlier contradiction (my old notes said "provisional filed"; cold-start brief said "discontinued"). Current truth: **dormant / stopped**.
- `PAPER_PLAN.md` and patent framing in `.kiro/specs/ml-architecture.md` are on hold — NOT active objectives.
- "Patent Claim N" strings in ML docstrings = legacy naming only. Do not build toward claims.
- Cold-start feature is justified purely on product grounds (greenfield-hospital day-1 calibration), not IP.
- All future decisions: product/engineering merit only.


## CRITICAL FACTS from research workspace (read end-to-end 2026-07-16)
Source: "PrescpHealth ML Design Patent and Academic Research Project" workspace root.

### Data — CONFIRMED IN HAND, license-cleared
- MIMIC-IV 3.1 full (364,627 patients) — downloaded + extracted. Train + internal test.
- eICU 2.0 — downloaded (zip). SOLE external performance-validation dataset (D001, user-confirmed).
- INSPIRE 1.4.2 (South Korea, perioperative) — extracted. SECONDARY validation only (surgical cohort).
- NHANES (many XPT files) — downloaded. NOT performance validation (self-reported labels, 5 of 6 diseases). Lifestyle EDA + Bayesian priors (cold-start) ONLY.
- WHO STEPS — priors/EDA only. Kaggle — RETIRED. MIMIC-BR — pending/maybe.
- PhysioNet CITI training DONE. Provisional patent really filed (#64/080,250).

### Patent/paper — CONFIRMED DISCONTINUED (PATENT_PAPER_TEARDOWN.md, 2026-07-09)
- Both dropped. Prior-art: all 8 claims anticipated (LOW-MEDIUM novelty). Provisional lapses ~June 2027. Product-only now.

### THE KEY GAP: training pipeline does NOT exist anywhere
- Research ml_pipeline/ = 4 config.py files ONLY (clean, survived reset). ICD-10 maps, MIMIC vital itemIDs, lab names, cohort criteria, candidate model list, NHANES var maps. ZERO implementation.
- EDA RESET 2026-07-05 (3 stats bugs). All D001-D013 numbers RETIRED. Raw data unaffected.
- Production ml/ = inference plumbing only, disease-agnostic (pattern C), NO trained models.
- => Two disconnected halves: DATA+CONFIGS (research) and INFERENCE PLUMBING (product). Building the training pipeline is the real missing work.

### Config alignment (good news)
- config.py DISEASE_ICD10_CODES = exactly the 6 production diseases. CANDIDATE_MODELS = xgboost,lightgbm,catboost,neural_net,tft,deepsurv (matches Option-3 minus TabNet).
- Compute: tiny prod box (98.87.133.69, 3.7GB, no GPU) ruled out. Some configs reference /home/ubuntu (data staged on AWS at some point). Dedicated training instance still to provision.
