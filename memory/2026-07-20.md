# 2026-07-20 — ML engine: real-model plumbing begins

## Reframe (user-corrected)
The "ML engine DONE (Tasks 20-22, checkpoint PASS)" claim was misleading. That
checkpoint only validated the **5-layer inference plumbing** with graceful
degradation. There are **no trained models** anywhere: every expert returns
`(0.5, 0.0)` and the meta-learner falls back to clinical-standard formulas. So
today the "ML engine" outputs guideline math, not ML. User confirmed the engine
is NOT done. Real work = the training half + wiring real artifacts.

Also found: orchestrator built ONE shared expert set labelled `"ensemble"` and
reused it for all 6 diseases — wrong for real per-disease models.

## Done this session (branch feat/emr-layer1, NOT yet committed)
1. **Per-disease orchestrator refactor + artifact-loading contract** (local, tested):
   - NEW `ml/risk_engine/model_registry.py` — `RiskModelRegistry`: builds per-disease
     expert ensembles, loads artifacts from `<root>/<disease>/<expert>/`, reads
     `metadata.json` for versions. `resolve_artifacts_root()` honors
     `PRESCP_ML_ARTIFACTS_ROOT` env. `has_trained_models()` + `disease_version()`
     for cold-start signals. Root=None → untrained experts → clinical fallback
     (preserves day-1 graceful degradation).
   - `ml/risk_engine/orchestrator.py` — `RiskOrchestrator(artifacts_root=None)`;
     `self._experts_by_disease` (per-disease, not shared); Layer-5 agreement now
     reuses Layer-2 probs (was buggily re-predicting on RAW features); model_versions
     per disease. Backward compatible (default None = old behavior).
   - NEW `ml/tests/test_model_registry.py` (7 tests) — incl. a real XGBoost
     train->save->load round trip verifying per-disease version isolation.
2. **MIMIC-IV cohort extraction scaffold** (pure logic + synthetic-CSV tests):
   - NEW `ml/training/data/paths.py` — env-driven MIMIC root (`PRESCP_MIMIC_ROOT`),
     `hosp_dir()`/`icu_dir()` with loud FileNotFoundError.
   - NEW `ml/training/data/disease_labels.py` — ICD-10 prefix -> 6-disease matrix.
     **ICD-9 rows deliberately excluded** (config prefixes are ICD-10 only);
     `icd9_diagnosis_count()` exposes the dropped fraction (discipline rule).
   - NEW `ml/training/data/mimic_cohort.py` — `build_cohort(patients, diagnoses)`
     pure core + `load_table`/`load_and_build_cohort` IO wrappers. Age gate from
     COHORT_CRITERIA; undiagnosed eligible patients kept as true negatives.
   - NEW `ml/tests/test_mimic_cohort.py` (13 tests).

## Verification
`python -m pytest ml/tests/ -q` -> **39 passed** (19 checkpoint + 7 registry + 13 cohort).
Clean diagnostics on all new files. Ran from repo root (needed for `import ml.*`).
All ML libs present locally (xgboost/lightgbm/catboost/torch/sklearn/pandas).

## NEXT (training pipeline, remaining)
- Feature engineering from labs/vitals (chartevents/labevents itemIDs in config)
  + apply remaining COHORT_CRITERIA (min_icu_hours, max_missing_vitals_pct).
- Temporal split by `anchor_year_group` (MIMIC exact dates not recoverable).
- Per-disease ensemble TRAINING (xgboost/lightgbm/catboost/neural -> weights),
  write artifacts in the registry layout the orchestrator now loads.
- Validation on **eICU only** (locked rule); calibration (ECE <= 0.05 floor).
- Cold-start: per-tenant persisted posteriors (BUILDER_COLD_START_BRIEF.md).
- Forecast engine training (tft/lstm/prophet/cox/deepsurv).
- Real MIMIC/eICU data + compute live on AWS box 98.90.192.78 (8 vCPU, no GPU) —
  pipeline code written here, executed there. NEED to confirm box is still live.

## Housekeeping
- Working tree still has the pre-session uncommitted doc reorg (patent/paper
  deletions, docs/, ml/training/config, SESSION_HANDOFF.md) PLUS today's new ML
  files. Not committed yet — offer to commit as a clean checkpoint.
- SESSION_HANDOFF.md is STALE (says Checkpoint 13). This note + 2026-07-16 are truth.
