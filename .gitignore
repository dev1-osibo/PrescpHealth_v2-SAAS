# =============================================================================
# PrescpHealth SaaS Platform — Git Ignore Rules
# =============================================================================
# Covers Python (backend + ML), Node.js (frontend), and common IDE files.
# CRITICAL: .env files with real secrets must NEVER be committed.
# =============================================================================

# =============================================================================
# ENVIRONMENT & SECRETS — Never commit real credentials
# =============================================================================
.env
.env.local
.env.production
.env.staging
*.pem
*.key
*.crt

# =============================================================================
# PYTHON — Backend & ML Engine
# =============================================================================

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/
ENV/

# Distribution / packaging
*.egg-info/
*.egg
dist/
build/
wheels/

# Package manager lock files (we DO commit these, but ignore generated ones)
# poetry.lock and uv.lock are committed — they are source of truth

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Mypy cache
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff cache
.ruff_cache/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# ML model artifacts (large binary files — stored in object storage, not git)
ml/artifacts/
*.pkl
*.joblib
*.h5
*.pt
*.pth
*.onnx
*.pmml

# =============================================================================
# NODE.JS — Frontend
# =============================================================================

# Dependencies
node_modules/

# Build output
frontend/dist/
frontend/build/

# Vite cache
frontend/.vite/

# Test coverage
frontend/coverage/

# Playwright test results
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/

# TypeScript build info
*.tsbuildinfo

# =============================================================================
# IDE & EDITOR FILES
# =============================================================================

# VS Code (keep .vscode/settings.json for shared workspace settings)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# JetBrains (PyCharm, WebStorm)
.idea/

# Vim
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# =============================================================================
# DOCKER
# =============================================================================
# Docker build context exclusions are in .dockerignore, not here.
# But we ignore local docker volumes and compose overrides.
docker-compose.override.yml
.docker-data/

# =============================================================================
# MISCELLANEOUS
# =============================================================================

# Logs (application logs should go to stdout/structured logging, not files)
*.log
logs/

# Temporary files
tmp/
temp/

# OS-generated files
*.bak
*.tmp
*.temp

# Database files (local SQLite for testing only)
*.db
*.sqlite3

# Generated documentation (rebuild from source)
docs/_build/
site/

# =============================================================================
# CREDENTIALED CLINICAL DATASETS — NEVER COMMIT (PhysioNet DUA / HIPAA)
# =============================================================================
# Raw datasets (MIMIC-IV, eICU, INSPIRE, NHANES, WHO STEPS) are credentialed
# under Data Use Agreements. They must NEVER enter version control, even a
# private repo — committing/pushing is redistribution and violates the DUA.
# Training runs on the AWS training instance where the data resides; only
# code, configs, and (gitignored) trained artifacts live here.
# Root-anchored (/data/) so it does NOT match the ml/training/data/ CODE package,
# which holds cohort-extraction logic (no data) and must stay tracked. Raw/processed
# datasets are blocked by the file-format patterns below and ml/training/processed/.
/data/
ml/data/
ml/training/processed/

# Raw clinical data file formats
*.XPT
*.xpt
*.csv.gz
*.parquet
*.dta
*.sas7bdat

# Dataset archives (large credentialed downloads)
*mimic*.zip
*eicu*.zip
*inspire*.zip
*nhanes*.zip
*who_steps*.zip
