# PrescpHealth Checkpoint QA Report
**Date:** 2026-05-31 19:24 EDT
**Verdict:** ❌ FAIL
**Run by:** QA Tester agent (qa-tester)

---

## Phase 1 — Existing Tests

- **Total:** 686
- **Passed:** 683
- **Failed:** 3
- **Coverage baseline:** 65.00%

**Failing tests:**

| File:Line | Test | Root Cause |
|---|---|---|
| `backend/tests/integration/test_ai_assistant_router.py:94` | `test_send_chat_message_returns_200` | `AttributeError: module 'uuid' has no attribute 'timezone'` at `backend/app/modules/ai_assistant/router.py:122` — should be `datetime.timezone.utc` not `uuid.timezone.utc`. Secondary: `TypeError: 'MagicMock' object can't be awaited` on `audit_service.log_action` |
| `backend/tests/integration/test_ai_assistant_router.py:132` | `test_get_chat_history_returns_200` | HTTP 500 "Failed to retrieve history" — downstream from above bug |
| `backend/tests/integration/test_forecast_router.py:92` | `test_trigger_forecast_returns_202_with_task_id` | HTTP 500 "Failed to enqueue forecast task" — `ForecastCompleted` called with wrong kwargs (see Bug #2 below) |

---

## Phase 2 — Integration Tests

- **RLS isolation (original 10 tables):** 10/10 PASS
- **Unique constraints:** 3/3 PASS
- **Foreign keys:** 16/16 PASS
- **Cross-module flows:** 4/4 PASS
- **Audit integrity:** PASS
- **Schema verification:** PASS
- **New RLS tests (10 new tables, agent-written):** 10/10 PASS

**New tenant-scoped tables discovered and tested (not in original AGENTS.md scope):**

| Table | Module |
|---|---|
| `users` | auth |
| `patient_versions` | patients |
| `conversations` | ai_assistant |
| `messages` | ai_assistant |
| `medication_records` | drug_interactions |
| `interaction_results` | drug_interactions |
| `risk_scores` | risk_engine |
| `shap_explanations` | risk_engine |
| `forecasts` | forecast_engine |
| `intervention_simulations` | forecast_engine |

**Phase 2 total: 50/50 PASS**

---

## Phase 3 — Coverage Gaps

208 new tests written. Final coverage: **68.00%** (894 tests, 891 pass, 3 fail).

**STRUCTURAL BLOCKER — 85% is mathematically unreachable without a production code fix:**

The 4 `*_staging` module groups define the same SQLAlchemy table names as their production
counterparts (same `Base.metadata`). Importing both in the same Python process raises
`InvalidRequestError: Table 'X' is already defined for this MetaData instance`.
Coverage.py cannot instrument them via importlib bypass.
They represent **1,207 statements (19.2% of total), all at 0%.**

| Staging group | Files | Stmts | Cover |
|---|---|---|---|
| `ai_assistant_staging` | 6 | 279 | 0% |
| `drug_interactions_staging` | 7 | 376 | 0% |
| `forecast_engine_staging` | 6 | 243 | 0% |
| `risk_engine_staging` | 8 | 309 | 0% |
| **Total** | **27** | **1,207** | **0%** |

Math:
- Total stmts: 6,276
- Staging (all 0%): 1,207
- Non-staging stmts: 5,069 — currently at **84.7% coverage**
- Maximum overall even if every non-staging line covered: **5,069 / 6,276 = 80.7%**
- **85.00% is impossible without fixing the staging MetaData conflict**

**Secondary blockers (non-staging, also at 0%):**

| Module | Stmts | Reason |
|---|---|---|
| `core/middleware/audit.py` | 53 | `redis` package not installed in dev environment |
| `core/middleware/rate_limit.py` | 46 | same |
| `core/middleware/__init__.py` | 4 | same |
| `auth/router.py` | 41 | Not registered in test app `create_app()` |
| `workers/celery_app.py` | 6 | No Celery test harness |
| `workers/beat_schedule.py` | 3 | same |

**Modules moved to 100% by new tests (net gains this run):**

| Module | Before | After |
|---|---|---|
| `drug_interactions/engine.py` | 13% | 100% |
| `drug_interactions/exceptions.py` | 0% | 100% |
| `risk_engine/exceptions.py` | 0% | 100% |
| `risk_engine/enums.py` | 63% | 100% |
| `forecast_engine/service.py` | 41% | 100% |

**Modules significantly improved:**

| Module | Before | After |
|---|---|---|
| `drug_interactions/service.py` | 18% | 83% |
| `risk_engine/service.py` | 32% | 76% |

**Remaining non-staging gaps (still open):**

| Module | Cover | Miss | Lines |
|---|---|---|---|
| `ai_assistant/service.py` | 26% | 49 | 59-61, 98-200, 219-237, 271-290, 304-313 |
| `ai_assistant/providers.py` | 35% | 75 | 61, 66, 95-131, 135, 159-195, 199, 228-258, 262, 303-336 |
| `forecast_engine/tasks.py` | 25% | 47 | 85-126, 164-198, 218-248, 269-296 |
| `risk_engine/tasks.py` | 26% | 46 | 86-178, 200-299, 304-311 |
| `drug_interactions/router.py` | 52% | 30 | 124-132, 205-213, 255-309, 359-392 |
| `encounters/router_detail.py` | 61% | 40 | 57-67, 85-100, 194-216, 260-279 |
| `lab_orders/service.py` | 74% | 20 | 362-390, 417-440 |
| `encounters/service_soap.py` | 75% | 13 | 163-194 |
| `core/cache.py` | 78% | 18 | 72-76, 99-100, 103, 119, 143, 171, 179, 205-214 |
| `patients/search.py` | 77% | 14 | 116, 121-122, 172-173, 181, 183, 207-217 |
| `measurements/history.py` | 76% | 12 | 90, 100, 102, 104, 106, 110-114, 130, 135-136 |

---

## Phase 4 — Security

- **PHI in test files:** NONE ✅
  - All MRNs are synthetic patterns (`MRN-RLS-PAT-001`, `MRN-XF-CHRONIC`, `MRN-AI-{hex}`, etc.)
  - No real names, SSNs, phone numbers, or DOBs found across all test files
- **PHI in exception messages:** NONE ✅
  - `DuplicateMRNError` — accepts `mrn` param, does **not echo it** in message or details (only emits `"field": "medical_record_number"`) ✅
  - All other exceptions use opaque UUIDs only ✅
  - `DrugCodeNotFoundError` echoes drug code — RxNorm/ATC codes are reference data, not PHI ✅
- **SQL injection:** ALL 11 BLOCKED ✅ — parameterized queries confirmed via real-DB injection tests

---

## Bugs Found in Production Code

> QA cannot fix these. Listed for the coding agent.

### Bug 1 — CRITICAL (causes test failure)
**File:** `backend/app/modules/ai_assistant/router.py:122`
```python
# Current (broken):
"timestamp": str(__import__("datetime").datetime.now(uuid.timezone.utc).isoformat()),

# Fix:
"timestamp": str(__import__("datetime").datetime.now(__import__("datetime").timezone.utc).isoformat()),
# Or cleaner — add `from datetime import datetime, timezone` at top of file and use:
"timestamp": datetime.now(timezone.utc).isoformat(),
```

### Bug 2 — CRITICAL (causes test failure)
**File:** `backend/app/modules/forecast_engine/service.py:252`
```python
# Current (broken) — ForecastCompleted does not accept these kwargs:
event = ForecastCompleted(
    patient_id=patient_id,
    forecast_id=forecast.id,
    target=target,              # ← not a field on ForecastCompleted
    horizon_months=horizon_months,  # ← not a field
    point_estimate=float(point_estimate),  # ← not a field
)

# Fix — ForecastCompleted only has patient_id and forecast_id:
event = ForecastCompleted(
    patient_id=patient_id,
    forecast_id=forecast.id,
)
```

### Bug 3 — LOW (DeprecationWarning, not a test failure)
**File:** `backend/app/modules/drug_interactions/router.py:120, 201`
```python
# Current:
"timestamp": datetime.utcnow().isoformat()

# Fix:
"timestamp": datetime.now(timezone.utc).isoformat()
```

### Bug 4 — CRITICAL (blocks 85% coverage threshold)
**All `*_staging/models.py` files**

Each staging model group reuses the same `Base` (and its `MetaData`) as the production modules
but defines tables with identical names (`model_versions`, `risk_scores`, `conversations`, etc.).
This causes `sqlalchemy.exc.InvalidRequestError: Table 'X' is already defined for this MetaData instance`
when both are imported in the same Python process.

**Fix options (pick one):**
```python
# Option A — extend_existing in __table_args__ for all staging models:
__table_args__ = (
    {"extend_existing": True},
)

# Option B — use a separate MetaData for staging:
# In backend/app/modules/ai_assistant_staging/models.py (and other staging models):
from sqlalchemy.orm import DeclarativeBase
class StagingBase(DeclarativeBase):
    pass
# Then inherit from StagingBase instead of Base
```

---

## New Test Files Written This Run

| File | Tests | Purpose |
|---|---|---|
| `backend/tests/integration/test_rls_new_tables.py` | 10 | RLS isolation for 10 new tenant-scoped tables |
| `backend/tests/unit/test_new_modules_schemas_exceptions_enums.py` | 129 | Schemas, exceptions, enums across new+staging modules |
| `backend/tests/unit/test_drug_interactions_engine.py` | 27 | InteractionEngine logic (pure Python + mocked DB) |
| `backend/tests/unit/test_service_unit_mocked.py` | 32 | DrugInteractionService + RiskService with async mocks |
| `backend/tests/unit/test_forecast_service_unit.py` | 10 | ForecastService with mocked Celery + events |

---

## Verdict

**FAIL** — two independent reasons:
- 3 test failures (zero tolerance policy)
- Coverage 68.00% < 85.00% — structurally unreachable without production code fix

## Recommended Next Actions (ordered by impact)

1. **Fix Bug 1** — `ai_assistant/router.py:122` — one-line fix, unblocks 2 failing tests immediately
2. **Fix Bug 2** — `forecast_engine/service.py:252` — remove 3 unsupported kwargs from `ForecastCompleted(...)`, unblocks 1 failing test
3. **Fix Bug 4 (staging MetaData conflict)** — this single fix pushes overall coverage from **68% → ~85%+** and clears the threshold blocker. See fix options above.
4. **Update `AGENTS.md` TENANT_SCOPED_TABLES list** — add: conversations, messages, medication_records, interaction_results, risk_scores, shap_explanations, forecasts, intervention_simulations (all RLS-confirmed this run)
5. **Register `auth/router.py` in test app conftest** — 41 stmts at 0%, no HTTP-layer auth endpoint tests
6. **`pip install redis` in dev environment** — unblocks 103 stmts in cache/middleware modules
